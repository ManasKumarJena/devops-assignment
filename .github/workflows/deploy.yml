name: DevOps Assignment Pipeline

on:
  push:
    branches:
      - main

  workflow_dispatch:
    inputs:
      confirm_destroy:
        description: "Type DESTROY_AND_DEPLOY to destroy and recreate infrastructure"
        required: true
        default: ""

jobs:
  deploy:
    runs-on: ubuntu-latest

    env:
      AWS_REGION: ap-south-1
      AWS_ACCESS_KEY_ID: ${{ secrets.AWS_ACCESS_KEY_ID }}
      AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRET_ACCESS_KEY }}

    steps:
    # 1️⃣ Checkout code
    - name: Checkout repository
      uses: actions/checkout@v4

    # 2️⃣ Validate manual trigger confirmation
    - name: Validate destroy confirmation
      if: github.event_name == 'workflow_dispatch'
      run: |
        if [ "${{ github.event.inputs.confirm_destroy }}" != "DESTROY_AND_DEPLOY" ]; then
          echo "❌ Invalid confirmation key. Aborting."
          exit 1
        fi
        echo "✅ Destroy confirmation validated."

    # 3️⃣ Setup Terraform
    - name: Setup Terraform
      uses: hashicorp/setup-terraform@v3

    # 4️⃣ Terraform Init
    - name: Terraform Init
      working-directory: terraform
      run: terraform init

    # 5️⃣ Terraform Destroy (manual trigger only)
    - name: Terraform Destroy
      if: github.event_name == 'workflow_dispatch'
      working-directory: terraform
      run: terraform destroy -auto-approve

    # 6️⃣ Terraform Apply
    - name: Terraform Apply
      working-directory: terraform
      run: terraform apply -auto-approve

    # 7️⃣ Deploy application using AWS SSM (NO SSH)
    - name: Deploy Docker app via SSM
      run: |
        aws ssm send-command \
          --region $AWS_REGION \
          --document-name "AWS-RunShellScript" \
          --targets "Key=tag:Name,Values=devops-assignment" \
          --parameters commands='[
            "sudo apt update -y",
            "sudo apt install -y docker.io git",
            "sudo systemctl enable docker",
            "sudo systemctl start docker",

            "cd /home/ssm-user",
            "rm -rf devops-assignment",
            "git clone https://github.com/${{ github.repository }}.git",
            "cd devops-assignment",

            "sudo docker build -t devops-app -f docker/Dockerfile .",
            "sudo docker rm -f devops-app || true",
            "sudo docker run -d -p 80:5000 --restart unless-stopped --name devops-app devops-app"
          ]'
